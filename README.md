# ai-pr-review
a GitHub webhook bot that inspects incoming Pull Request diffs, uses LLM/AST parsing to detect potential bugs, edge-case failures, or security risks, and leaves inline comments.

source/venv/bin/activate
deactivate